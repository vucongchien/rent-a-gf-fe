services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: rent-a-gf-fe:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env.local
    restart: always
